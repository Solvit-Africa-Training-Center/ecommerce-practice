{
  "watch": ["src"],
  "ext": "ts,js,json",
  "ignore": ["src/**/*.spec.ts", "src/**/*.test.ts", "node_modules"],
  "exec": "ts-node src/index.ts",
  "events": {
    "restart": "echo \"[nodemon] App restarted due to changes\"",
    "crash": "echo \"[nodemon] App crashed - waiting for file changes before restarting\""
  },
  "delay": "500",
  "env": {
    "NODE_ENV": "DEV"
  }
}